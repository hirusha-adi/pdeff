# pdeff

Offline, client-side PDF annotation app. It keeps the PDF unchanged and stores
annotations/comments/view state in a same-folder `.pdfa` sidecar when the browser
supports the File System Access API.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
